# Sample Foundry Project

This directory contains a minimal Foundry project that ships with the API container. Feel free to replace the
contracts, tests, and configuration with your own. The Bun server runs `forge test -vvvv` from this folder by default.

```bash
cd foundry
forge test -vvvv
```
